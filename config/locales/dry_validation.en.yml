en:
  dry_validation:
    errors:
      address:
        rules:
          address:
            format?: "Only allows a-z, A-Z, 0-9,’,’, ‘-’, ‘ ’"
          city:
            format?: "Only allows a-z, ' ', A-Z"
          first_name:
            format?: 'Only allows letters'
          last_name:
            format?: 'Only allows letters'
          no_association:
            'Either order_id or user_id must be present'
          phone:
            format?: "Only allows '+', 0-9"
          zip:
            format?: "Only allows 0-9 '-'"
