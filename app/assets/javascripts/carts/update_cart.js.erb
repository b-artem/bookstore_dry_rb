$(document).on('turbolinks:load', updateCart = function() {
  var ary = [1, 2, 3]
  $.ajax({
        url : "/books/3",
        type : "get"

    });


  // var quantity;
  // quantity = $(event.target).parent().siblings('.quantity-input').val();
  // if ( quantity == '1' ) {
  //   return false;
  // } else {
  //   quantity--;
  // }
  // $(event.target).parent().siblings('.quantity-input').val(quantity);
});
